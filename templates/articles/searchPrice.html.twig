{% extends 'base.html.twig' %}
{% block title %}Recherche par prix{% endblock %}

{% block body %}
<h2 class="mb-4">Recherche par prix</h2>

<div class="row mb-4">
  <div class="col-md-6">
    {{ form_start(form) }}
    <div class="mb-3">
      {{ form_label(form.minPrice) }}
      {{ form_widget(form.minPrice) }}
    </div>
    <div class="mb-3">
      {{ form_label(form.maxPrice) }}
      {{ form_widget(form.maxPrice) }}
    </div>
    <button type="submit" class="btn btn-primary">Rechercher</button>
    {{ form_end(form) }}
  </div>
</div>

{% if articles is not empty %}
  <h4>Résultats ({{ articles|length }})</h4>
  <div class="row">
    {% for article in articles %}
      <div class="col-md-4 mb-4">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title">{{ article.nom }}</h5>
            <p class="card-text">Prix : {{ article.prix }} DT</p>
            {% if article.category %}
              <span class="badge bg-primary">{{ article.category.titre }}</span>
            {% endif %}
            <div class="mt-2">
              <a href="{{ path('article_show', {id: article.id}) }}" class="btn btn-sm btn-info">Voir</a>
            </div>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
{% elseif form.vars.submitted %}
  <div class="alert alert-warning">Aucun article trouvé dans cette fourchette de prix.</div>
{% endif %}

<a href="{{ path('article_list') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
